---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<article class={`rounded-[1.8rem] border border-border/70 bg-card/95 p-5 shadow-sm transition hover:-translate-y-0.5 hover:shadow-panel ${className}`.trim()}>
  <slot />
</article>
