---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`overflow-hidden rounded-[2rem] border border-border/70 bg-card shadow-panel ${className}`.trim()}>
  <slot />
</div>
